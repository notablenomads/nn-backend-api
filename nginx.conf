# HTTP server for ACME challenge and redirect
server {
    listen 80;
    server_name api.notablenomads.com;

    # ACME challenge location for SSL certificate verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    # Health check endpoint
    location = /nginx-health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 'healthy\n';
    }

    # API endpoints
    location / {
        proxy_pass http://api:3000;
        proxy_http_version 1.1;
        
        # Essential headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Production timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS server - temporarily disabled until we have SSL certificates
# server {
#     listen 443 ssl;
#     server_name api.notablenomads.com;
# 
#     # SSL configuration
#     ssl_certificate /etc/nginx/ssl/live/api.notablenomads.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/api.notablenomads.com/privkey.pem;
#     ssl_trusted_certificate /etc/nginx/ssl/live/api.notablenomads.com/chain.pem;
# 
#     # SSL security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options DENY;
#     add_header X-Content-Type-Options nosniff;
#     add_header X-XSS-Protection "1; mode=block";
# 
#     # Health check endpoint
#     location = /nginx-health {
#         access_log off;
#         add_header Content-Type text/plain;
#         return 200 'healthy\n';
#     }
# 
#     # API endpoints
#     location / {
#         proxy_pass http://api:3000;
#         proxy_http_version 1.1;
#         
#         # Essential headers
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         
#         # Production timeouts
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
# 
#         # Handle connection errors gracefully
#         proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
#         proxy_next_upstream_tries 3;
#         proxy_next_upstream_timeout 10s;
#     }
# }