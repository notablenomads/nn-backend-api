# The manifest for the "platform" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: platform
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  healthcheck:
    path: '/v1/health'
    success_codes: '200,301'
    healthy_threshold: 2
    interval: 30s
    timeout: 10s
    grace_period: 60s

# Configuration for your containers and service.
image:
  # Docker build arguments. For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#image-build
  build: Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 3000

# Resource configuration
cpu: 256 # 0.25 vCPU
memory: 512 # 0.5 GB
platform: linux/arm64 # ARM for better price/performance

# Single instance configuration
count: 1 # Fixed single instance

exec: true # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

variables: # Pass environment variables as key value pairs.
  APP_NAME: Notable Nomads Backend API
  HOST: 0.0.0.0
  PORT: 3000
  CORS: 'true'
  API_PREFIX: v1
  RELEASE: 0.0.1
  # Development defaults (overridden by environment variables)
  NODE_ENV: development
  CORS_ENABLED_DOMAINS: 'localhost:3000,localhost:4200'
  CORS_RESTRICT: 'false'
  EMAIL_FROM_ADDRESS: 'no-reply@mail.notablenomads.com'

# Store sensitive values in AWS Systems Manager Parameter Store
secrets:
  GEMINI_API_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/GEMINI_API_KEY
  AWS_ACCESS_KEY_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_ACCESS_KEY_ID
  AWS_SECRET_ACCESS_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_SECRET_ACCESS_KEY

environments:
  production:
    # Minimal production settings with high availability
    count: 1 # Single instance
    variables:
      NODE_ENV: production
      CORS_ENABLED_DOMAINS: '*.notablenomads.com,notablenomads.com,api.notablenomads.com'
      CORS_RESTRICT: 'false'
  staging:
    # Minimal staging settings
    count: 1 # Single instance
    variables:
      NODE_ENV: staging
      CORS_ENABLED_DOMAINS: '*.staging.notablenomads.com,staging.notablenomads.com,api.staging.notablenomads.com'
      CORS_RESTRICT: 'false'
